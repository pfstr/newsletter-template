{
  "name": "newsletter-template",
  "version": "1.0.0",
  "description": "Serverless newsletter on your own Cloudflare account — signup, compliant one-click unsubscribe, and campaigns to your own list. Bring your own email sender.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "predeploy": "wrangler d1 migrations apply DB --remote",
    "dev": "npm run seedLocalD1 && wrangler dev",
    "seedLocalD1": "wrangler d1 migrations apply DB --local",
    "test:e2e": "playwright test",
    "test:e2e:codegen": "playwright codegen http://localhost:8787"
  },
  "cloudflare": {
    "label": "Newsletter",
    "products": [
      "Workers",
      "D1"
    ],
    "bindings": {
      "ADMIN_TOKEN": {
        "description": "Password that protects your send page (/admin). Use a long, random string."
      },
      "FROM_NAME": {
        "description": "The name your emails are sent from, e.g. \"Jane's Newsletter\"."
      },
      "FROM_EMAIL": {
        "description": "The address your emails are sent from, once you connect an email provider in src/email.ts."
      },
      "DOUBLE_OPT_IN": {
        "description": "Set to \"true\" to require subscribers to confirm via a link before they're added (needs email configured). Default \"false\" (single opt-in)."
      }
    }
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "@playwright/test": "^1.61.1",
    "typescript": "5.9.3",
    "wrangler": "4.88.0"
  }
}
